# PostgreSQL Configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d20u18re5dus7388a0v0-a.oregon-postgres.render.com:5432/cloud_fund_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:cloud_fund_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:mL6Xuc48KpPPubIEexv0FlIir2ORJ8M0}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# JWT
jwt.secret=${JWT_SECRET:myProductionSecretKeyShouldBeLongerAndMoreSecure}
jwt.expiration=${JWT_EXPIRATION:86400}

# EMAIL CONFIG
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:info.sairuraldevelopmenttrust@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vyrjuzftuavkshyv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Razorpay
razorpay.key.id=${RAZORPAY_KEY_ID:rzp_test_a3kb1GXuvUpqcu}
razorpay.key.secret=${RAZORPAY_KEY_SECRET:Xor0KhdlApRxLBS9EZLSgj1p}

# Logging
logging.level.com.donorbox=INFO
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=WARN
logging.level.root=INFO

# Swagger
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.enabled=true

# Server
server.port=${PORT:8080}

# Uploads
app.upload.dir=uploads
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
spring.servlet.multipart.file-size-threshold=2KB

# Admin Email
admin.email=info.sairuraldevelopmenttrust@gmail.com
